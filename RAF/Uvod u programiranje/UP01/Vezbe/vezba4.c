#include <stdio.h>
#include <stdlib.h>
/*Ispisati vrednost malog i velikog slova A i njihovu razliku. */

int main(){
	//inicijalizacija malog i velikog slova A
	char a = 'a';
	char A = 'A';
	printf("Vrednost malog slova a je %d\n", a);
	printf("Vrednost velikog slova A je %d\n", A);
	printf("Razlika izmedju a i A je %d\n",a-A);

	return 0;
}